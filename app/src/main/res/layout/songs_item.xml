<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/ItemBoard"
    android:layout_margin="5dp"
    android:paddingTop="5dp"
    android:paddingBottom="5dp"
    android:id="@+id/board">

    <ImageView
        android:id="@+id/icon"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_centerVertical="true"
        android:src="@drawable/music_icon" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/music_title_text"
        android:padding="2dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="Music"
        android:textColor="@color/black"
        android:layout_toEndOf="@+id/icon"
        android:layout_toStartOf="@+id/music_title_duration"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/music_second_text"
        android:padding="2dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textSize="10sp"
        android:text="Dir"
        android:textColor="#9E000000"
        android:layout_toEndOf="@+id/icon"
        android:layout_below="@+id/music_title_text"
        android:layout_toStartOf="@+id/music_title_duration"
        tools:ignore="SmallSp" />

    <TextView
        android:id="@+id/music_title_duration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_alignParentRight="true"
        android:ellipsize="end"
        android:maxLines="1"
        android:padding="10dp"
        android:text="00:00"
        android:textColor="@color/black" />
</RelativeLayout>